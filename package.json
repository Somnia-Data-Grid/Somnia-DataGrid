{
  "name": "somnia-datagrid",
  "version": "1.0.0",
  "private": true,
  "description": "Somnia DataGrid - Shared market data streams for the Somnia ecosystem",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:workers\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:workers": "cd workers && npm run dev",
    "build": "npm run build:frontend && npm run build:workers",
    "build:frontend": "cd frontend && npm run build",
    "build:workers": "cd workers && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../workers && npm install",
    "db:migrate": "cd workers && npm run db:migrate"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
